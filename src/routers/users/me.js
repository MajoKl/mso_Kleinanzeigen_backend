const { Router } = require("express");
const { model } = require("mongoose");

const router = Router();

router.post("/create", () => {});

router.get("/", (req, res) => {});

module.exports = router;
